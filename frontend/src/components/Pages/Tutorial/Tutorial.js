import { Typography } from '@mui/material';
import MuiTable from './MuiTable';

export default function Home() {
  return (
    <div>
      <Typography variant="body1">Ejemplo de Tabla</Typography>
      <MuiTable />
    </div>
  );
}
